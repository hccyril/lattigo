# 密码学论文及代码学习入门指导文档

## 1. 论文背景与动机 (Research Background)

**全同态加密 (Fully Homomorphic Encryption, FHE)** 允许在不解密的情况下直接对密文进行计算。在过去的十多年中，FHE方案得到了极大的优化，主要可以分为基于 LWE 的方案（如 CGGI/TFHE，具有非常快的布尔门计算速度）和基于 RLWE 的方案（如 BGV/BFV 以及 **CKKS**，具有极高的吞吐量和并发计算能力，特别是 CKKS 擅长浮点数和实数的近似计算）。

**研究动机：**
传统上，学术界一直认为不同的 FHE 家族适用于不同的场景。例如 TFHE 擅长计算无符号大整数（如 64-bit 整数），而 CKKS 只能用于实数的乘加运算。但是，随着**离散 CKKS (Discrete CKKS)** 及其 Bootstrapping（自举）技术的发展，CKKS 现在可以利用多项式插值精确计算任意函数（例如模运算）。这篇论文 **"Efficient Homomorphic Integer Computer from CKKS"** 提出了一个极其有趣的问题：**能否让本来只擅长实数近似计算的 CKKS 方案，也能高效处理标准的 64-bit 整数并且实现超越 TFHE 的高吞吐量？**

## 2. 论文核心贡献与研究方法 (Contributions & Methodology)

### 2.1 核心贡献
论文提出了一种在离散 CKKS 之上构建的高效的全同态计算机架构，专门用于**无符号大整数（如 64-bit）的各类运算**。
* **高吞吐量：** 实现的 64-bit 乘法每 slot 的均摊执行时间仅为 8.85 毫秒，比成熟的 TFHE-rs 库快数千倍。
* **支持运算丰富：** 传统上基于 CKKS 很难做比较操作和位运算，论文不仅实现了加法和乘法，还实现了同态比较和同态移位操作。
* **核心思想：大整数的进制分解 (Digit-Decomposition)。** 不直接将 64-bit 整数塞到一个槽 (slot) 里加密，而是将其分解为较小的进制块（例如将 64-bit 分解为 16 个 4-bit 的数字）。

### 2.2 研究方法
1. **数字分解：** 取基数 $d$（如 $d=16$ 即 4-bit），将目标模数 $t = d^u$ 的大整数表示为 $a = \sum_{i=0}^{u-1} a_i \cdot d^i$。并用离散的形式分别对这些 $a_i$ 块进行 CKKS 密文加密。
2. **同态模运算机制：** 利用最新论文（如 KN25）在 CKKS 底层模数上使用自举（Bootstrapping）实现精确的模 $d$（$IntMod_d$）和进位运算（$Carry_d$）能力。
3. **延迟进位/延迟减少技巧：** 为了避免因不断除以 $d$（进位消耗）而过快消耗密文层级，作者精心设计了一种“惰性（Lazy）”更新的算法，使得在不引起计算溢出（Noise Overflow）的前提下，实现总体开销最小的 FHE 加法和乘法。

## 3. 核心公式与运作机制 (Core Formulas)

在这个系统中，最重要的过程是 **进位计算和规约 (Reduction)**。由于对不同位数（块）加密，需要处理低位向高位的“进位”逻辑。

### 3.1 进位 (Carry) 与 模 (IntMod) 提取
如果有一个数字 $c_i$，我们需要提取出留位部分(即模 $d$) $r_i$ 和进位部分(即除 $d$) $q_i$。在数学上：
* **$IntMod_d$ 提取余数：** $r_i = [c_i]_d$ （保留在当前位）
* **$Carry_d$ 提取进位：** $q_i = \lfloor c_i / d \rfloor = (c_i - [c_i]_d) / d$ （进给下一位）

### 3.2 规约算法 (Reduction)
这是所有运算的最终步骤，目的是将运算（加法或乘法）中可能超出限制位数的“脏密文”还原回正常的 $[0, d-1]$ 范围：
* 设输入向量 $c = (c_0, c_1, ..., c_{u-1})$
* **Step 0:** $r_0 = IntMod_d(c_0)$,  $q_0 = Carry_d(c_0)$
* **Step i:** 针对原本的第 $i$ 块 $c_i$ 加上前一位供上来的进位 $q_{i-1}$，即计算 $c_i' = c_i + q_{i-1}$
  * 然后拆分：$r_i = IntMod_d(c_i')$,  $q_i = Carry_d(c_i')$
* **输出：** 最终的每一位即为干净的 $(r_0, r_1, ..., r_{u-1})$

### 3.3 乘法 (Multiplication)核心公式
对于 $a = \sum a_i d^i$ 和 $b = \sum b_i d^i$，它们的乘法等价于多项式乘积（卷积）：
$$ [ab]_{d^u} =  \sum_{i=0}^{u-1} \left( \sum_{j=0}^{i} a_j b_{i-j} \right) \cdot d^i $$
程序中首先利用 CKKS 底层的 `eval.Mult` 将各个子块对应交叉相乘并累加，然后统一进行一次上述的 **Reduction** 操作来处理所有的错位与进位。

### 3.4 比较与移位操作
* **比较大小 (Comparison)：** 计算 $a \ge b$ 被转化为计算特殊的减法。将 $a-b$ 计算后，如果最高位结果为 0 表示 $\ge$（$a \ge b$），如果为 -1 (由于 FHE 表达为负数或溢出) 则为 $<$。
* **移位操作 (Shift)：** 移位直接转化为数组下标的偏移量（例如移动倍数是 $d$ 则直接整体平移位数），再加上对于未对齐数位的 $Carry_d$ 的计算。

---

## 4. 代码实现与论文章节的对应关系 (Code Mapping)

项目的基础代码依赖于 `Lattigo 6.1.0`（这是一个主要用 Go 编写的极速晶格密码学库）。在此之上，新增了针对本文“大整数计算机架构”的各种原语与实例代码。

### 核心实验目录：`examples/singleparty/ckks_bootstrapping/slim/`
所有的核心 FHE 大整数计算脚本都放置于该目录下，包含了对各个基本运算的测试：

* **加法运算 (`add.go`)**
  * **对应论文：** 第 3.2 节的 Addition 部分。
  * **主要流程：** 将两个加密大数拆分为 4-bit 块；对每一位分别调用基本的密文加法进行同态相加（未重整进位）；接着调用自定义的 `Reduction()` 引擎执行自底向上的 $IntMod$ 和进位 $Carry$ 调整；最后通过 Lattigo 自带 Bootstrapping 解决尺度 (scale) 问题。
* **减法运算 (`sub.go`)** 
  * **对应论文：** 第 3.2 节的 Subtraction。逻辑类似于加法。
* **乘法及其扩展 (`mult.go`, `mult-256.go`, `mult-ext-256.go`)** 
  * **对应论文：** 第 3.2 节的 Multiplication 算法，以及第 3.4.1/4.3 节的定点运算和大素数扩展运算应用（如 Curve25519 的大数乘法）。
  * **主要流程：** 实现卷积乘法逻辑。先将 $a_j$ 和 $b_{i-j}$ 乘起来，求出第 $i$ 位的聚合值；然后再统一调用带 Bootstrapping 的 `Reduction()` 更新进位。
* **比较大小 (`compare.go`)**
  * **对应论文：** 第 3.2 节 Comparison。使用携带符号的减法最高位判断。
* **位移运算 (`left-shift.go`, `right-shift.go`)**
  * **对应论文：** 第 3.2 节 Shift。

### 框架修改点 (`circuits/` & `utils/`) 
为了支撑这种“离散形式”运算体系，作者修改了底层的计算管线：
* **`circuits/ckks/bootstrapping/evaluator.go`**: 新增或修改了 `EvalMod`, `EvalElse`, `EvalModAndScale` 等方法。这对应论文 `IntModd` 的底座构建（利用复指数与查找表 `LUT` 处理模块边界问题）。
* **`utils/sampling/sampling.go`**: 新增了一些方便离散浮点采样的辅助函数 `RandModFloat`, `RandFloat128`。

## 5. 挑战与学习建议
接下来，我们将在具体的关键代码文件里加入详细的中英对照注释（包含你要求的代码解析和公式对应），帮助你越过纯数理障碍。建议你打开上述的 `.go` 文件对照这篇大纲学习。
